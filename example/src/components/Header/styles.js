import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => {
    return {
        title: {
            color: 'inherit',
            textDecoration: 'none',
        },
    };
});
